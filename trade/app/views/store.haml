%h2
  Store
%p
  Your credits: $#{@user.credits}
%h3
  Items currently in store:
%table.table-minimalistic
  %thead
    %tr
      %th Item
      %th Price
      %th Owner
      %th
  %tbody
    -for user in users
      -for item in user.get_active_items
        %tr
          %td
            %a{:href => url("/item/#{item.id}")} #{item.name}
          %td
            #{item.price}
          %td
            %a{:href => url("/user/#{item.owner.name}")} #{item.owner.name}
          %td
            %div.action_buttons
              -if user != @user
                #button_container
                  %form{:action => "/user/buy/#{item.id}", :method => "POST"}
                    %input.styled-button.small.buy-color{:type => "submit", :value => "Buy"}
%h3
  Recent purchases
- if most_recent_purchases.empty?
  %p No recent purchases
-else
  %table.table-minimalistic
    %tbody
      -for activity in most_recent_purchases
        %tr
          %td
            =activity.timestamp.to_s
          %td
            =activity.what_happened

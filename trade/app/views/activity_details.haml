%h2
  Activity Details
%p
  At #{activity.timestamp}, user
  - if actor_still_in_system
    %a.underlined{:href => "/user/#{actor.name}"} #{actor.name}
  - else
    =activity.actor_name
  did the following modifications to item
  - if item_still_in_system
    %a.underlined{:href => "/item/#{item.id}"} #{item.name}
  - else
    =activity.item_name

- case activity.type
  - when :ITEM_ADD
    ToDo
  - when :ITEM_BUY
    ToDo
  - when :ITEM_DELETE
    ToDo
  - when :ITEM_STATUS_CHANGE
    ToDo
  - when :ITEM_EDIT
    ToDo
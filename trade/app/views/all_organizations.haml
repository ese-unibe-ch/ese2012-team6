%div#actionbar
  %h2.float_left
    Organizations
  %div.action_buttons.float_right
    %a.styled-button{:href => "/organization/new"} Create new Organization
%h3
  The following organizations are registered by the system
%table.table-minimalistic
  %thead
    %tr
      %th Pic
      %th Name
      %th Credits
      %th # Members
      %th No. of items
  %tbody
    -for organization in Store::Organization.all
      %tr
        %td
          %img{:src => organization.image_path, :height => "40px"}
        %td
          %a{:href => "/organization/#{organization.name}"}
            :escaped
              #{organization.name}
        %td.ralign
          #{organization.credits}
        %td.ralign
          #{organization.members.size}
        %td.ralign
          #{organization.items.size}
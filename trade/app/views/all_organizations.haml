%h2
  Organizations
%h3
  The following organizations are registered by the system
%table.table-minimalistic
  %thead
    %tr
      %th Name
      %th Credits
      %th No. of items
  %tbody
    -for organization in Store::User.all
    -if  organization.is_organization? do{
      %tr
        %td
          %a{:href => url("/organization/#{organization.name}")}
            :escaped
              #{organization.name}
        %td.ralign
          #{organization.credits}
        %td.ralign
          #{organization.items.size}}

